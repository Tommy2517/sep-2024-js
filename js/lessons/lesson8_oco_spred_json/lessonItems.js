// 1. optional chain operator - 'Elvis' = user?.name?.func
// 2. create object from variables let name, age = 'Havard', 33; let user = {name, age}
// 3. destructurization let {name:userName, age:userAge} = user; let [user1, ,user2] = users[user1,boss, user2] (ignore boss = ", ,")
// 4. copy objects and arrays = let newObj = {...oldObj}.// let newObj =  JSON.parse(JSON.stringify(oldObj)) // Object.assign({куди}, user) < з 
// 5. prototyping. let obj = {id:1, name:'qqq'} let obj2 = obj.__proto__. (obj2 === {}) obj2.id === 1. obj2.name === qqq. 
// 6. description. 